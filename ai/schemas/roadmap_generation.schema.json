{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "GenerateRoadmapResponse",
  "description": "Response shape for ai/prompts/generate_roadmap.py. Every resource_id MUST come from the candidate set passed into the prompt — never invented.",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "resource_id": { "type": "string" },
          "order": { "type": "integer" },
          "milestone": { "type": "boolean" },
          "reason": { "type": "string", "description": "1-2 sentences grounded in the specific candidate resource + this learner's profile" }
        },
        "required": ["resource_id", "order", "milestone", "reason"]
      }
    }
  },
  "required": ["items"]
}
